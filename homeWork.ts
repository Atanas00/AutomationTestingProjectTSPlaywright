let name:String; 
let age:number;
name= "Petar Petrov";
age= 25; 
console.log(`Hello! I am ${name}, ${age} years old!`); 

let num1:number = 5;
let num2:number = 15;
const result:number =num1+num2;
console.log(result);

const MIN_RANGE:number = 10;
const MAX_RANGE:number = 50

let isWithinRange: boolean = num1 >= MIN_RANGE || num2 <= MAX_RANGE;
console.log(`Is the number within the range (${MIN_RANGE}-${MAX_RANGE})? ${isWithinRange}`);

